alias config-rebuild="pushd . && cd <<CONFIG_REPO_PATH>> && ./build_config.sh && sudo cp generated/configuration.nix /etc/nixos/ && cp generated/flake.nix /etc/nixos/ && sudo nixos-rebuild --flake /etc/nixos#palladion switch --impure && popd"
